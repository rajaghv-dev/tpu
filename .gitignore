__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.env
.venv/
venv/
.tpu/
*.ipynb_checkpoints/
checkpoints/
logs/
data/
wandb/
*.tfevents.*
.claude/

# OTel run output — per-run JSONL is regeneratable; runs.jsonl in results/ stays tracked.
results/otel/
otelcol-contrib
otelcol-contrib_*.tar.gz

# Runtime state from staged scripts/ pipeline (TPU name, zone, deploy timestamps)
.tpu-bench-state/
# Per-run tmux tee logs from 50_run_smoke.sh / 51_run_quick.sh
results/*.log
